{
  "name": "my-vercel-project-shankar",
  "version": "1.0.0",
  "description": "A secure project to handle form submissions using Vercel serverless functions.",
  "main": "api/contact.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build step required'",
    "start": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@aws-sdk/client-ses": "^3.x",
    "ua-parser-js": "^1.0.35",
    "express-rate-limit": "^6.7.0",
    "helmet": "^7.0.0",
    "xss": "^1.0.14",
    "email-validator": "^2.0.4"
  },
  "engines": {
    "node": "20.x"
  }
}
